dependencies:
stow git git-cli make gcc neovim picom clang base-devel unzip ripgrep fd alacritty cmake 

install lazyvim

installation:
git clone https://github.com/your-username/dotfiles.git ~/dotfiles

cd ~/dotfiles

stow .
